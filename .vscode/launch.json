{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Listen for Xdebug",
            "port": 9003,
            "type": "php",
            "log": true,
            "request": "launch",
            "pathMappings": {
                "/var/www/html": "${workspaceFolder}"
            },
        }
    ]
}